<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.13.1/font/bootstrap-icons.min.css">
</head>
<body>
    <div class="container pt-5">
        <div class="d-flex justify-content-between align-items-center mb-5">
            <h2>Users List</h2>
            <a href="/add-user" class="btn btn-primary fw-bold">
                <i class="bi bi-person-fill-add fs-5"></i>
                add new user</a>
        </div>

        <table class="table table-bordered text-center shadow ">
            <thead class="table-dark" >
                <tr>
                    <th>Sr.no</th>
                    <th>First Name</th>
                    <th>Last Name</th>
                    <th>Email</th>
                    <th>PH.No</th>
                    <!-- <th>Address</th> -->
                    <th>Action</th>
                </tr>
            </thead>
            
            <tbody class="table-light">
                <% var srno=1 %>
                <% User_info.forEach(UserData=>{%>
                <tr>
                    <td><%= srno++ %></td>
                    <td><%= UserData.fname %> </td>
                    <td><%= UserData.lname %></td>
                    <td><%= UserData.email %></td>
                    <td><%= UserData.phno %></td>
                    
                    <td>
                        <a href="/show-user/<%= UserData._id %>" class="text-info me-2" title="show"><i class="bi bi-eye-fill"></i></a>
                        <a href="/update-user/<%= UserData._id %>" class="text-warning me-2" title="Edit"><i class="bi bi-pencil"></i></a>
                        <a href="/delete-user/<%= UserData._id %>" class="text-danger me-2 "><i class="bi bi-trash-fill" title="Delete"></i></a>
                    </td>
                </tr>
                <%});%>
            </tbody>
        </table>
    </div>


    <!-- js bundle -->
     <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>