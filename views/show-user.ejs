<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Show Contact/User</title>
     <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.13.1/font/bootstrap-icons.min.css">
   
</head>
<body>
    <div class="container mt-5">
        <div class="card shadow-lg" style="max-width: 700px; margin:auto;">
            <div class="card-header bg-primary text-white text-center">
                <h3>User Detail</h3>
            </div>

            <div class="card-body">
                <div class="mb3">
                    <strong>First Name : </strong><%= oneUserData.fname %>
                </div>
                <div class="mb3">
                     <strong>Last Name : </strong><%= oneUserData.lname %>
                </div>
                <div class="mb3">
                     <strong>Email : </strong><%= oneUserData.email %>
                </div>
                <div class="mb3">
                     <strong>Phone : </strong><%= oneUserData.phno %>
                </div>
                <div class="mb3">
                     <strong>Address : </strong><%= oneUserData.address %>
                </div>
            </div>
            <div class="d-flex gap-4 my-2 ms-2">
                <a href="/update-user/:id" class="btn btn-warning px-3">Edit</a>
                <a href="/delete-user" class="btn btn-danger px-3">Delete</a>
                <a href="/" class="btn btn-secondary px-3">Cancel</a>
            </div>
        </div>
    </div>
</body>
</html>